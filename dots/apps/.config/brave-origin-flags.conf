# Brave Origin (Chromium) : Wayland natif, decodage video par la 4090 (VA-API via nvidia-vaapi-driver)
--ozone-platform-hint=auto
--enable-features=WaylandLinuxDrmSyncobj,VaapiVideoDecodeLinuxGL,VaapiIgnoreDriverChecks
--enable-wayland-ime

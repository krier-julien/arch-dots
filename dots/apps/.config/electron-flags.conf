# Flags pour les apps Electron utilisant l'electron systeme (vesktop, pear-desktop...).
# Wayland natif + syncobj (explicit sync) : plus de scintillement sur NVIDIA.
--ozone-platform-hint=auto
--enable-features=WaylandLinuxDrmSyncobj,WaylandWindowDecorations
--enable-wayland-ime
